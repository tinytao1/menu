//document.write('\"this from X-functions.js file\"');

/*
function selectPrototype() {

  section.item.displayCount();
}



function selectPrototype() {

    if (s1.count == s1.limit) {
    s1.count = s1.count - this.count;
    this.count = 0;
    this.selected = false;
    // s1.reactivateAllItemButtons();
    this.displayCount();
  }
  else {
    this.count++;
    s1.count++;
    this.selected = true;
    //s1.displayCount();
  }
  if (s1.count == s1.limit) { 
    // deactivateAllBtns();
    // s1.reactivateSelectedItemButtons();
    //s1.displayCount();
  }
}
*/